In the 2d sketch view, it should not be possible to change the angle of the camera. Just the location, zoom in/out.
In the 3d view, there should be a "view cube" like in fusion 360, letting you choose specific views, like xy, xz, at the middle angle, etc...

Also, a bug: choose a 2d view, it tries to fix the problem of having the sketch perpendicular to the grid plane, so it changes the position of the grid plane. But when you go back to the 3d view, it doesn't get fixed. In general, the 2d view should not be using the same grid system, as it's 2d, not 3d.
Also, the sketch grid is invisible from bottom, only visible from above.
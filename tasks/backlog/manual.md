automatic update of the files
surface history view from UI
create point A, point B, move object so A and B align
update the view only when the code is valid, otherwise wait until the code becomes valid
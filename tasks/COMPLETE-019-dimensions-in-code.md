So currently the LLM can build quite cool models, you can check the examples.
But there doesn't seem to be a way to add dimensions into the code for rendering.
Currently the user can add dimensions manually, but that's not as nice.
Let's make it so that the LLM can both build the model and add some dimensions for the user to see, for example for knowing how to cut the wood, etc. Not a constraint, purely for reporting.
Make it a nice and easy API, maybe integrating with existing APIs of creating a line from two dots, etc. Or maybe even add a dimension on top of a given edge, etc.
BTW, make the visuals nice and clean, like with Fusion360, with some margin from the actual dots/line.